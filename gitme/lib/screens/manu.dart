import 'package:flutter/material.dart';

class ManuScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Manufacture Screen'),
    );
  }
}